services:
  - type: web
    name: agrotech-backend
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    # Migraciones y admin en el arranque: funciona también en el plan gratuito (sin pre-deploy).
    startCommand: python manage.py migrate --noinput && python manage.py ensure_admin && gunicorn agrotech.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.4
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: agrotech-campo-ciudad.onrender.com
      - key: CORS_ALLOWED_ORIGINS
        value: https://agrotech-campo-ciudad-qfzf-kq9jtt1qu-estebanfrms-projects.vercel.app
      - key: CSRF_TRUSTED_ORIGINS
        value: https://agrotech-campo-ciudad.onrender.com,https://agrotech-campo-ciudad-qfzf-kq9jtt1qu-estebanfrms-projects.vercel.app
      - key: CORS_ALLOW_CREDENTIALS
        value: "True"
      # Credenciales del administrador: Render las pide al crear el Blueprint.
      - key: ADMIN_EMAIL
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: agrotech-db
          property: connectionString
      - key: SECURE_SSL_REDIRECT
        value: "True"
      - key: SESSION_COOKIE_SECURE
        value: "True"
      - key: CSRF_COOKIE_SECURE
        value: "True"
      - key: SECURE_HSTS_SECONDS
        value: "31536000"

databases:
  - name: agrotech-db
    databaseName: agrotech
    user: agrotech
